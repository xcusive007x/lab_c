#include <stdio.h>
int main()
{
    int a;
    float f;
    char s;
    printf("enter value: ");
   // scanf("%d", &a);
   // scanf("%f", &f);
   scanf("%s", s);
   // printf("value is %d", a);
   // print("value is %.2f", f);
   printf("valuse is %s", s);
    return 0;
}